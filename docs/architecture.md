# Sentinel-NET Architecture

Sentinel-NET monitors a deployed Random Forest fraud classifier. Each transaction
passes through the classifier, the monitoring pipeline records operational and
model-output signals, and an Isolation Forest scores the monitoring event.

SQLite stores monitoring events, drift events, and alerts. Streamlit reads those
events and displays model health, live anomaly scores, drift status, and model
metrics generated by training scripts.
